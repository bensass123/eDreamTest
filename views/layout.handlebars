<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <!-- Favicon -->
    <link rel="shortcut icon" href="./img/favicon/favicon.ico">
    <!-- Reset CSS -->
    <link rel="stylesheet" href="./css/reset.css" type="text/css">
    <!-- Main Style CSS -->
    <link rel="stylesheet" href="./css/style.css" type="text/css">
    <!-- Roboto Google Font -->
    <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
    <!-- jQuery -->
    <script type="text/javascript" src="https://code.jquery.com/jquery-3.2.1.js"></script>
    <!-- YouTube API-->
    <script type="text/javascript" src="http://www.youtube.com/player_api"></script>
    <script type="text/hbs" src="views/test.hbs"></script>
    <style>
      #buttons a {
        padding: 20px;
      }
    </style>
  </head>
  <body>
    {{{body}}}
    
  </body>
  <script>
  // console.log($(location).attr('pathname'));
  // var h = $(location).attr('pathname');
  // var p = h.lastIndexOf('/');
  // var address = h.slice(p+1,h.length);
  // console.log(address);
  // var nextLink = $('<a/>', {
  //         text: 'Next', //set text 1 to 10
  //         id: 'next',
  //         href: parseInt(address) + 1 
  //     });
  // var prevLink = $('<a/>', {
  //         text: 'Previous', //set text 1 to 10
  //         id: 'previous',
  //         href: parseInt(address) - 1 
  //     });
  // $('#buttons').append(prevLink);
  // $('#buttons').append(nextLink);  

  function getJ(ind){
    $.getJSON(ind, function(data){
        var nextLink = $('<a/>', {
          text: 'Next One', //set text 1 to 10
          id: 'next',
          href: ind + 1 
      });  
    }
  }
  // var nextLink = $('<a/>', {
  //         text: 'Next', //set text 1 to 10
  //         id: 'next',
  //         href: parseInt(address) + 1 
  //     });  

  </script>
</html>

 

